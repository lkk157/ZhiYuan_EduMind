# 知源 ZhiYuan_EduMind · 协作规范（CLAUDE.md）

本文件是「知源」项目的协作契约。人与 AI 都必须遵守，冲突时以本文件为准。

## 1. 角色与分工
- **用户 = 产品经理 / 指挥官**：定需求、排优先级、验收、确认节奏。代码开发经验不足——技术方案必须解释原因。用户负责在本地部署环境执行命令并反馈报错。
- **Claude = 技术总监 / 执行者**：输出完整代码、架构设计、调试方案、测试脚本。不擅自扩需求、不擅自换技术栈。
- 一切变更先过产品经理确认。

## 2. 开发节奏（强制五步，违反即停）
每完成一个功能/阶段，必须按此节奏推进：
1. **思路**：讲清「做什么 + 为什么这么做 + 目录/文件变动清单」；
2. **确认**：产品经理确认后才动手（默认不可开工）；
3. **代码**：只写本阶段文件。关键代码必须带**详细中文注释**，注释要解释「**为什么这么做**」而不只是「做了什么」；
4. **测试**：提供单元测试 + 可复制粘贴的手动验证命令；
5. **提交**：测试确认无误后，git commit + push 到 GitHub。

★ 禁止一口气生成所有代码 / 一个阶段只做一个阶段的事。
★ 禁止在未确认时创建计划外文件或改动目录结构。

## 3. 硬件红线（RTX 4060 8G 显存 / 16G 内存，答辩同机演示）
任何设计不得违反以下红线（防 CUDA / 显存溢出）：
- 任一时刻大模型（LLM / OCR）**只能有一类活跃**：全局推理互斥锁 + `asyncio.Semaphore` 串行化，禁止并发打 Ollama；
- OCR 用完即卸（`OCR_KEEP_ALIVE=0`），入库前先卸 LLM，二者**时段隔离**（OCR 只在入库流水线触发）；
- `LLM_NUM_CTX ≤ 4096`，输出长度受控，embedding 批量限流（如一次 16 条）；
- 提交前自查：新增代码是否可能引入并发推理或常驻大模型？不可能才允许提交。

## 4. 技术栈锁定（替换需产品经理书面确认）
FastAPI / Streamlit / MySQL 8.x / ChromaDB / LangGraph /
Ollama（LLM: Qwen2.5:7b（本机实际标签 `qwen2.5:7b-instruct-q4_K_M`）；视觉OCR: GLM-OCR（glm-ocr）；Embedding: Qwen3-Embedding:0.6b）/
JWT（PyJWT）/ pytest。

## 5. 代码质量约定
- 关键函数/类必须中文 docstring，写「为什么」而不只是「做了什么」；
- 配置一律走 `.env` + `app/core/config.py`，禁止硬编码密码/模型名/阈值；模型名以 `ollama list` 输出的 NAME 为准填入 `.env`；
- 新模块必须配套 `tests/` 单测，**无测试不算完成**；
- Windows 开发：文件统一 UTF-8（无 BOM），路径一律 `pathlib`，文件读写显式 `encoding="utf-8"`。

## 6. Git 提交门禁（每阶段）
- **单元测试全部通过**（`pytest -q` 全量，不是只跑新测试）才允许提交；
- commit message 格式：`<type>(<scope>): <中文摘要>`（type ∈ feat/fix/docs/test/chore）；
- push 到 `git@github.com:lkk157/ZhiYuan_EduMind.git`（main 分支）；
- 里程碑完成额外打 tag（如 `M3-thin-slice-demo`）。

## 7. 禁止事项
- 禁止自由发挥扩需求 / 换技术栈；
- 禁止生成无中文注释的关键逻辑；
- 禁止一次性生成整个项目的所有代码；
- 禁止引入 Docker / Redis 等本机无法运行的依赖（生产方案只写进 README 对比表，不写实现）；
- 禁止任何有显存溢出风险的设计（见第 3 节红线）。

## 8. 阶段过程报告（每阶段完成后必写）
每完成一个阶段（里程碑），必须写一份「项目过程报告」，积累过程文档与论文素材：
- **存放位置**：`docs/reports/<阶段编号>-过程报告.md`（如 `docs/reports/M1-过程报告.md`），文件名与标题注明所属阶段；
- **固定三段结构**：
  1. **这阶段做了什么**（交付物与关键实现）；
  2. **遇到了什么问题**（按条目列出，含环境/代码/流程问题）；
  3. **怎么解决的问题**（与问题条目一一对应的解决动作与结论）；
- **时机**：与该阶段的单元测试、git commit/push 同步完成——过程报告文件本身随该阶段一并提交推送；
- 补写历史阶段报告同样入库（文首注明「补写」与实际所属阶段）。
